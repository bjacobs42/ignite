[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 16:00 UTC = 18:00 CEST (UTC+2, summer) / 17:00 CET (UTC+1, winter)
[functions."sync-products"]
  schedule = "0 16 * * *"
  timeout  = 60

[functions."test-sync"]
  timeout = 30
